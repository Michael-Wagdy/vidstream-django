# 🎬 منصة بث الفيديو بالدجانجو - شرح كامل

## 📋 نظرة عامة على المشروع

منصة بث فيديو احترافية تحول ملفات MP4 إلى صيغة HLS للبث التكيفي مع تتبع حالة المعالجة في الوقت الفعلي.

---

## 🏗️ نمط الهندسة المعمارية

### المكونات الأساسية
```
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│   سيرفر      │    │   سيلري      │    │    ريديس       │
│   الدجانجو    │◄──►│   العامل       │◄──►│   الوسيط       │
│   (REST API)   │    │   (المعالجة     │    │   (الطابور)     │
│               │    │   الخلفية)     │    │               │
└─────────────────┘    └─────────────────┘              │
                                                   ▼
                                            ┌─────────────────┐
                                            │   إف إم بي ج    │
                                            │   (المحوّل)     │
                                            └─────────────────┘
```

### تدفق البيانات
```
رفع المستخدم → إدارة الدجانجو → مهمة سيلري → إف إم بي ج → ملفات HLS → مشغل فيديو جي إس
       │              │              │           │           │
       ▼              ▼              ▼           ▼
   ملف MP4 → سجل البيانات → معالجة خلفية → أجزاء → بث مباشر
                    السجل           الخلفية      (.m3u8/.ts)
```

---

## 🎯 الميزات الرئيسية

### 1. **معالجة الفيديو الذكية**
- ✅ منع المهام المكررة
- ✅ تتبع الحالة (قيد الانتظار → قيد المعالجة → مكتمل/فشل)
- ✅ إعادة المحاولة التلقائية مع تراجع أسي
- ✅ استخراج الصور المصغرة عند 10 ثواني
- ✅ كشف المدة والبيانات الوصفية

### 2. **مزايا بث HLS**
- 🚀 البث التكيفي حسب معدل البت
- 📱 التوافق عبر المنصات
- ⚡ وقت بدء سريع
- 🔄 التبديل التلقائي للجودة
- 📊 تحسين النطاق الترددي

### 3. **جاهز للإنتاج**
- 🐳 حاوية عبر دوكر
- 🔒 رؤوس الأمان وتحديد المعدل
- 📊 تسجيل شامل
- 🏥 فحوص الصحة والمراقبة
- 🌐 وكيل عكسي لـ إنجنكس جاهز

---

## 🔧 التنفيذ التقني

### نمط نموذج الفيديو
```python
class Video(models.Model):
    id = models.UUIDField(primary_key=True)
    title = models.CharField(max_length=200)
    original_file = models.FileField(upload_to=get_video_upload_path)
    processed_m3u8 = models.FileField(upload_to=get_hls_upload_path)
    status = models.CharField(choices=STATUS_CHOICES, default='pending')
    
    # تشغيل المعالجة الذكي
    def save(self, *args, **kwargs):
        # تشغيل فقط للفيديوهات الجديدة بملفات
        # منع المعالجة المكررة
```

### نمط مهمة سيلري
```python
@shared_task(bind=True, max_retries=3)
def process_video_task(self, video_id):
    # التحقق إذا كان قيد المعالجة
    # تحويل إف إم بي ج مع معالجة الأخطاء
    # إعادة المحاولة التلقائية عند الفشل
    # تحديثات الحالة خلال العملية
```

### نمط أمر إف إم بي ج
```bash
ffmpeg -i input.mp4 \
  -c:v libx264 -c:a aac \
  -b:v 1000k -b:a 128k \
  -vf scale=-2:720 \
  -f hls -hls_time 10 \
  -hls_list_size 0 \
  -hls_segment_filename segment_%03d.ts \
  output.m3u8
```

---

## 📁 هيكل المشروع

```
django-video-streaming/
├── config/                 # مشروع الدجانجو + إعدادات سيلري
├── videos/                 # تطبيق الفيديو
│   ├── models.py          # نموذج الفيديو + حفظ ذكي
│   ├── tasks.py           # المعالجة الخلفية
│   ├── serializers.py      # تسلسل واجهة برمجة التطبيقات
│   ├── views.py           # نقاط نهاية REST
│   └── admin.py          # واجهة الإدارة
├── templates/videos/        # قوالب الواجهة الأمامية
├── media/                # رفعات المستخدمين (حجم دوكر)
├── static/                # الملفات الثابتة
├── Dockerfile             # تعريف الحاوية
├── docker-compose.yml     # مكدسة التطوير
├── docker-compose.prod.yml # مكدسة الإنتاج
└── nginx.conf             # وكيل عكسي للإنتاج
```

---

## 🚀 أنماط النشر

### مكدسة التطوير
```yaml
services:
  web:       # سيرفر تطوير الدجانجو
  worker:    # عامل سيلري
  celery-beat: # المهام المجدولة
  db:         # بوستجرس إس كيو إل
  redis:       # وسيط الرسائل
```

### مكدسة الإنتاج
```yaml
services:
  web:       # جونيكورن + الدجانجو
  worker:    # عامل سيلري
  nginx:      # وكيل عكسي + تقديم الملفات الثابتة
  db:         # بوستجرس إس كيو إل
  redis:       # وسيط الرسائل
```

---

## 📊 المراقبة والرصد

### فحوص الصحة
- اختبارات اتصال قاعدة البيانات
- اختبارات بنغ ريديس
- حالة عامل سيلري
- مراقبة عمليات إف إم بي ج

### استراتيجية التسجيل
```
logs/
├── django.log        # سجلات سيرفر الويب
├── celery.log        # سجلات العامل
└── error.log         # تتبع الأخطاء
```

### المقاييس
- وقت معالجة الفيديو
- معدل نجاح التحويل
- عمق الطابور
- استخدام الذاكرة/وحدة المعالجة المركزية

---

## 🔒 أنماط الأمان

### المصادقة
- إدارة الدجانجو للرفع
- وصول لواجهة برمجة التطبيقات بالرموز
- تكوين كورس

### أمان الملفات
- أسماء الملفات المعتمدة على UUID
- منع اجتياز المسار
- التحقق من نوع الملف
- حدود الحجم (500 ميجابايت)

### أمان الشبكة
- تحديد المعدل (واجهة برمجة التطبيقات: 10 طلب/ث، فيديو: 5 طلب/ث)
- رؤوس الأمان
- فرض إتش تي تي بي إس
- تعقيل المدخلات

---

## 📈 تحسينات الأداء

### قاعدة البيانات
- مفاتح أساسية UUID
- استعلامات محسّنة
    - تجميع الاتصالات
- فهرسة على حقل الحالة

### التخزين المؤقت
- ريديس لتخزين الجلسات
- تخزين مؤقت للملفات الثابتة
- تخزين مؤقت لاستجابات واجهة برمجة التطبيقات

### معالجة الفيديو
- حالات إف إم بي ج متوازية
- حجم الأجزاء الفعال (10 ثواني)
- جاهز لتسريع العتاد

---

## 🌐 تصميم واجهة برمجة التطبيقات

### نقاط النهاية
```
GET /api/videos/              # قائمة الفيديوهات (مقسمة)
GET /api/videos/{id}/          # تفاصيل الفيديو
GET /api/videos/{id}/stream/  # معلومات البث
GET /api/videos/stats/         # الإحصائيات
```

### تنسيق الاستجابة
```json
{
  "count": 20,
  "results": [
    {
      "id": "uuid",
      "title": "عنوان الفيديو",
      "status": "مكتمل",
      "hls_url": "/media/videos/hls/uuid/video.m3u8",
      "thumbnail_url": "/media/videos/hls/uuid/thumbnail.jpg",
      "duration": 120.5,
      "duration_display": "02:00"
    }
  ]
}
```

---

## 🎯 حالات الاستخدام

### 1. **المنصات التعليمية**
- تسجيل المحاضرات
- مواد الدورات
- تقديمات الفيديو للطلاب

### 2. **تدريب الشركات**
- فيديوهات تدريب الموظفين
- عروض المنتجات
- الاتصالات الداخلية

### 3. **شركات الإعلام**
- منظمات الأخبار
- منشئي المحتوى
- خدمات استضافة الفيديو

### 4. **منصات التعلم الإلكتروني**
- الدورات عبر الإنترنت
- محتوى التعليمات
- تطوير المهارات

---

## 🔮 أنماط القابلية للتوسع

### دعم الجودات المتعددة
```python
QUALITY_PRESETS = {
    '360p': {'scale': '640:360', 'bitrate': '500k'},
    '720p': {'scale': '1280:720', 'bitrate': '1000k'},
    '1080p': {'scale': '1920:1080', 'bitrate': '2500k'}
}
```

### الميزات المتقدمة
- تكامل البث المباشر
- تحليلات الفيديو
- تعليقات المستخدمين/التقييمات
- إدارة قوائم التشغيل
- دعم الترجمات
- تكامل إدارة الحقوق الرقمية

---

## 📚 التقنيات الرئيسية

| المكون | التقنية | الغرض |
|---------|----------|--------|
| الخلفية | الدجانجو + DRF | واجهة برمجة التطبيقات، الإدارة |
| الطابور | سيلري + ريديس | المعالجة الخلفية |
| التحويل | إف إم بي ج | تحويل الفيديو |
| قاعدة البيانات | بوستجرس إس كيو إل | استمرارية البيانات |
| الواجهة الأمامية | فيديو جي إس | تشغيل HLS |
| الحاوية | دوكر | النشر |
| الوكيل | إنجنكس | تقديم الإنتاج |

---

هذا النمط يوفر أساساً شاملاً ومكتملاً لمنصة بث الفيديو يمكن تخصيصها لحالات الاستخدام المحددة مع الحفاظ على قابلية التوسع والموثوقية.
